version: "3"

services:
  # Test database.
  database:
    image: postgres
    environment:
      POSTGRES_HOST_AUTH_METHOD: "trust"
    ports:
      - 25432:5432

  # Test container.
  test:
    image: anikalaw/clerk:${TAG}
    links:
      - database
    env_file:
      - ../env/.env.test
