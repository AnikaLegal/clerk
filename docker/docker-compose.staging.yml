version: "3"

services:
  web:
    command: make web-prod
    image: anikalaw/clerk:latest
    ports:
      - 8001:8001
    volumes:
      - /var/run/postgresql:/app/postgres.sock
      - /var/log/clerk/test/web:/var/log
    environment:
      DJANGO_SETTINGS_MODULE: clerk.settings.staging
      GUNICORN_PORT: 8001
      PGHOST: /app/postgres.sock
      PGPORT: 5432
      PGDATABASE: clerk-test
      PGUSER: $PGUSER
      PGPASSWORD: $PGPASSWORD
      # Pass through
      DJANGO_SECRET_KEY: $DJANGO_SECRET_KEY
      SENDGRID_API_KEY: $SENDGRID_API_KEY
      AWS_ACCESS_KEY_ID: $AWS_ACCESS_KEY_ID
      AWS_SECRET_ACCESS_KEY: $AWS_SECRET_ACCESS_KEY
      RAVEN_DSN: $RAVEN_DSN
      PAPERTRAIL_HOSTNAME: ClerkTest
      PAPERTRAIL_PORT: $PAPERTRAIL_PORT
      PAPERTRAIL_URL: $PAPERTRAIL_URL

  worker:
    command: make worker-prod
    image: anikalaw/clerk:latest
    volumes:
      - /var/run/postgresql:/app/postgres.sock
      - /var/log/clerk/test/worker:/var/log
    environment:
      DJANGO_SETTINGS_MODULE: clerk.settings.staging
      PGHOST: /app/postgres.sock
      PGPORT: 5432
      PGDATABASE: clerk-test
      PGUSER: $PGUSER
      PGPASSWORD: $PGPASSWORD
      # Pass through
      DJANGO_SECRET_KEY: $DJANGO_SECRET_KEY
      SENDGRID_API_KEY: $SENDGRID_API_KEY
      AWS_ACCESS_KEY_ID: $AWS_ACCESS_KEY_ID
      AWS_SECRET_ACCESS_KEY: $AWS_SECRET_ACCESS_KEY
      RAVEN_DSN: $RAVEN_DSN
      PAPERTRAIL_HOSTNAME: ClerkTest
      PAPERTRAIL_PORT: $PAPERTRAIL_PORT
      PAPERTRAIL_URL: $PAPERTRAIL_URL
      PUBLIC_SUBMIT_SLACK_WEBHOOK_URL: $PUBLIC_SUBMIT_SLACK_WEBHOOK_URL
      PRIVATE_SUBMIT_SLACK_WEBHOOK_URL: $PRIVATE_SUBMIT_SLACK_WEBHOOK_URL
