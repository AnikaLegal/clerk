allOf:
  - $ref: ./TaskCommentBase.yaml
  - type: object
    properties:
      id:
        type: integer
      task_id:
        type: number
      creator:
        type: object
        properties:
          id:
            type: integer
          full_name:
            type: string
          url:
            type: string
        required:
          - id
          - full_name
          - url
      created_at:
        type: string
      modified_at:
        type: string
    required:
      - id
      - task_id
      - creator
      - created_at
      - modified_at
