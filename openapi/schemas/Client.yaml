allOf:
  - $ref: ./ClientBase.yaml
  - type: object
    properties:
      id:
        type: string
        format: uuid
      url:
        type: string
      age:
        type: integer
      full_name:
        type: string

    required:
      - id
      - url
      - age
      - full_name
      - date_of_birth
