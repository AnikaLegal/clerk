type: object
properties:
  has_full_access:
    type: boolean
  issues_with_access:
    type: array
    items:
      $ref: ./Issue.yaml
  issues_without_access:
    type: array
    items:
      $ref: ./Issue.yaml
required:
  - has_full_access
  - issues_with_access
  - issues_without_access
