allOf:
  - $ref: ./TaskBase.yaml
  - type: object
    properties:
      id:
        type: integer
      type:
        type: string
      status:
        type: string
      url:
        type: string
      issue:
        $ref: ./Issue.yaml
      owner:
        $ref: ./User.yaml
      assigned_to:
        $ref: ./User.yaml
    required:
      - id
      - url
      - type
      - issue
      - owner
      - assigned_to
      - status
