type: object
properties:
  status:
    type: string
  requesting_task:
    type: object
    properties:
      is_approved:
        type: boolean
      comment:
        type: string
    required:
      - is_approved
required:
  - status
  - requesting_task
