get:
  operationId: getTaskActivity
  parameters:
    - description: Entity ID
      in: path
      name: id
      required: true
      schema:
        type: number
  responses:
    "200":
      description: Successful response.
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: ../schemas/TaskActivity.yaml
