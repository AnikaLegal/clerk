get:
  operationId: getCases
  parameters:
    - name: page
      in: query
      schema:
        type: integer
      required: false
    - name: search
      in: query
      schema:
        type: string
      required: false
    - name: topic
      in: query
      schema:
        type: string
      required: false
    - name: stage
      in: query
      schema:
        type: string
      required: false
    - name: outcome
      in: query
      schema:
        type: string
      required: false
    - name: is_open
      in: query
      schema:
        type: string
      required: false
    - name: paralegal
      in: query
      schema:
        type: string
      required: false
    - name: lawyer
      in: query
      schema:
        type: string
      required: false
  responses:
    "200":
      description: Successful response.
      content:
        application/json:
          schema:
            $ref: ../responses/CaseListResponse.yaml
