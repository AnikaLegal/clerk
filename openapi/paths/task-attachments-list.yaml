get:
  operationId: getTaskAttachments
  parameters:
    - description: Entity ID
      in: path
      name: id
      required: true
      schema:
        type: number
  responses:
    "200":
      description: Successful response.
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: ../schemas/TaskAttachment.yaml
post:
  operationId: createTaskAttachment
  parameters:
    - description: Entity ID
      in: path
      name: id
      required: true
      schema:
        type: number
  requestBody:
    required: true
    description: Successful response.
    content:
      multipart/form-data:
        schema:
          $ref: ../schemas/TaskAttachmentCreate.yaml
  responses:
    "201":
      description: Successful response.
      content:
        application/json:
          schema:
            $ref: ../schemas/TaskAttachment.yaml
    "400":
      $ref: ../responses/BadRequest.yaml
    "403":
      $ref: ../responses/PermissionDenied.yaml
